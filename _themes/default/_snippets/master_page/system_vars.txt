<!-- system_vars -->
<script type="text/javascript" language="javascript">
var FP_SHOW_CAPTIONS = "{FP_SHOW_CAPTIONS}";
var showCaptions = "{FP_SHOW_CAPTIONS}";

var FPSHOWMATTED = ("{FP_SHOW_MATTED}" > 0 ? 1 : 0);
var MATTECOLOR = "{MATTECOLOR}";
var FPMatteRatio = FPSHOWMATTED * (1 - {MATTESCALE});
FPMatteRatio = FPMatteRatio.toFixed(2);
var FPMatteBottomRatio = FPSHOWMATTED * {MATTEBOTTOM};
FPMatteBottomRatio = FPMatteBottomRatio.toFixed(2);


var FP_LIGHTBOX_DISPLAY_REDUCTION = {FP_LIGHTBOX_DISPLAY_REDUCTION};
var FP_LIGHTBOX_DISPLAY_PADDING = {FP_LIGHTBOX_DISPLAY_PADDING};

var FPAnimateResize = true;

var FP_GALLERY_TYPE = "{FP_GALLERY_TYPE}";

var FP_DEFAULT_WIDTH = {FP_DEFAULT_WIDTH};
var FP_DEFAULT_HEIGHT = {FP_DEFAULT_HEIGHT};
var FP_SLIDE_WIDTH = {FP_SLIDE_WIDTH};
var FP_SLIDE_HEIGHT = {FP_SLIDE_HEIGHT};

var FP_GALLERY_URL = "http://{SYSTEMURL}index.php?forceshow=1&fp_user={USERID}";

var FP_SPACE_FOR_CAPTION = true;

var FP_GALLERY_MAX_PIC_HEIGHT = FP_DEFAULT_HEIGHT;
var FP_GALLERY_MAX_PIC_WIDTH = FP_DEFAULT_WIDTH;

var FP_SINGLE_GALLERY_SINGLE_USER = "{FP_SINGLE_GALLERY_SINGLE_USER}";
var FP_SINGLE_GALLERY_MULTI_USER = "{FP_SINGLE_GALLERY_MULTI_USER}";
var FP_MULTI_GALLERY_SINGLE_USER = "{FP_MULTI_GALLERY_SINGLE_USER}";
var FP_MULTI_GALLERY_MULTI_USER = "{FP_MULTI_GALLERY_MULTI_USER}";

var FP_THEMESDIR = "{THEMESDIR}";
var FP_DEFAULT_THEME = "{DEFAULT_THEME}";
var FP_THEME = "{THEME}";
var FP_THEME_ID = "{THEME_ID}";
var FP_THEME_NAME = "{THEME_NAME}";

var FP_WEB_FONT_LIST = "{FP_WEB_FONT_LIST}";

var FP_PROJECT_ID = "{PROJECT_ID}";
var FP_GROUP_ID = "{GROUPID}";

var FP_USER_ID = "{USERID}";
var FP_BKGD_RESIZE_NOCROP = false;	// default for themes
var FP_SET_BKGD_PICTURE = true;		// default for themes
var FP_RESIZE_TO_BKGD_IMAGE = false;	// set the window to fit the background image

var FP_CURRENT_IMAGE_INDEX = (('{FP_CURRENT_IMAGE_INDEX}' == '') ? 0 : {FP_CURRENT_IMAGE_INDEX});

var FP_SHIPPER_CODE = "{FP_SHIPPER_CODE}";
var FP_CURRENCY = "{CURRENCY}";
var FP_CURRENCY_AFTER = "{CURRENCY_AFTER}";
var FP_CURRENCY_POSITION = "{CURRENCY_POSITION}";

var FP_CUSTOMER_PICKUP_TEXT = "{FP_CUSTOMER_PICKUP_TEXT}";

var FP_SELL_MATTE_NO_FRAME = "{FP_SELL_MATTE_NO_FRAME}";
var FP_CUSTOMER_CHOOSES_PAPER = "{FP_CUSTOMER_CHOOSES_PAPER}";
var FP_MAX_FRAMED_SIZE = "{FP_MAX_FRAMED_SIZE}";

var FP_RANDOM_IMG_LIST = [ {RANDOM_IMG_LIST} ];

var FP_PAGE_TYPE = "{sectionclass}";

var FP_SOLIDIFY = "{FP_SOLIDIFY}";

var FP_SHADOWS = "{FP_SHADOWS}";

</script>
